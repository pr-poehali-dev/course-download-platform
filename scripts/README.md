# 🔍 Скрипты проверки системы

## Быстрый старт

### 0. 🧪 Тест системы покупок (НОВОЕ!)

**Визуальный тест в браузере** (самый простой):
1. Открой файл `scripts/test-purchase.html` в браузере
2. Нажми "Проверить баланс"
3. Нажми "Купить работу за 600 баллов"
4. Смотри результат в реальном времени

**Python скрипт** (автоматический):
```bash
python3 scripts/test-purchase.py
```

**Bash скрипт** (быстрый):
```bash
bash scripts/quick-test.sh
```

---

### 1. Полная проверка сайта
```bash
python3 scripts/system-audit.py
```

Эта команда запустит комплексную проверку:
- ✅ Базовая функциональность (backend, endpoints)
- ✅ Система баллов (начисление, списание, защита)
- ✅ Покупки и товары (endpoint, логика цен)
- ✅ Интеграция с Тинькофф (конфиг, webhook, безопасность)
- ✅ Безопасность (CORS, обработка ошибок)

### Результат:
```
🚀 ПОЛНАЯ ПРОВЕРКА РАБОТЫ САЙТА И СИСТЕМЫ БАЛЛОВ
==================================================================

📋 БАЗОВАЯ ФУНКЦИОНАЛЬНОСТЬ
==================================================================
   Проверка доступности backend функций...
✅ Все 4 backend функции доступны
   Проверка функции платежей...
✅ Payment endpoint работает
   Проверка функции работ...
✅ Works endpoint работает (15 работ)
✅ Категория пройдена: 3/3

...

📊 ИТОГОВАЯ СВОДКА
==================================================================
Всего тестов: 15
Пройдено: 14
Провалено: 1
Процент успеха: 93.3%

🎉 Сайт работает ОТЛИЧНО!

✅ Отчет сохранен: audit_report_20251121_154523.json
```

---

## Что проверяется

### 1. Базовая функциональность
- **Backend доступность**: Проверяет, что все backend функции отвечают
- **Payment endpoint**: Проверяет работу функции платежей
- **Works endpoint**: Проверяет получение списка работ

### 2. Система баллов
- **Логика начисления**: Проверяет правильность расчета кешбека (1%)
- **Логика списания**: Проверяет корректность списания баллов
- **Защита от минуса**: Проверяет, что баланс не может уйти в отрицательное значение

### 3. Покупки и товары
- **Purchase endpoint**: Проверяет доступность функции покупок
- **Логика цен**: Проверяет корректность расчета итоговой цены (баллы + деньги)

### 4. Интеграция с Тинькофф
- **Конфигурация**: Проверяет наличие terminal_key
- **Формат webhook**: Проверяет обязательные поля (Status, PaymentId, Token, etc.)
- **Безопасность**: Проверяет наличие защиты (проверка подписи, идемпотентность)

### 5. Безопасность
- **CORS заголовки**: Проверяет правильность настройки CORS
- **Обработка ошибок**: Проверяет корректность ответов при ошибках

---

## Интерпретация результатов

### ✅ Успех (90-100%)
```
🎉 Сайт работает ОТЛИЧНО!
```
**Действия**: Никаких. Все функционирует корректно.

### ⚠️ Предупреждение (70-89%)
```
⚠️ Есть незначительные проблемы
```
**Действия**: 
1. Проверьте отчет `audit_report_*.json`
2. Найдите тесты со статусом `warning` или `failed`
3. Исправьте проблемы в соответствующих файлах

### 🚨 Критическая проблема (<70%)
```
🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ!
```
**Действия**:
1. **СРОЧНО** проверьте отчет
2. Исправьте критические ошибки:
   - Недоступные backend функции
   - Ошибки в логике баллов
   - Проблемы с платежами
3. Запустите проверку повторно

---

## Примеры использования

### Быстрая проверка после изменений
```bash
# После изменения кода платежей
python3 scripts/system-audit.py

# Проверьте раздел "Интеграция с Тинькофф"
```

### Регулярный мониторинг
```bash
# Добавьте в cron для ежедневной проверки
0 9 * * * cd /path/to/project && python3 scripts/system-audit.py >> audit.log 2>&1
```

### Отладка конкретной проблемы
```bash
# Запустите скрипт и найдите провалившийся тест
python3 scripts/system-audit.py

# Откройте сгенерированный JSON отчет для деталей
cat audit_report_*.json | jq '.categories.["Система баллов"]'
```

---

## Структура отчета

Отчет сохраняется в JSON формате:

```json
{
  "timestamp": "2025-11-21T15:45:23.123456",
  "categories": {
    "Базовая функциональность": {
      "status": "completed",
      "tests": {
        "backend_availability": {
          "status": "passed",
          "available": 4,
          "total": 4
        },
        "payment_endpoint": {
          "status": "passed",
          "has_config": true
        }
      }
    }
  }
}
```

---

## Расширение скрипта

### Добавить свой тест

```python
def check_my_feature() -> Dict[str, Any]:
    """Проверка моей функции"""
    try:
        # Ваша логика проверки
        result = some_check()
        
        if result:
            print_success("Моя функция работает")
            return {'status': 'passed'}
        else:
            print_fail("Моя функция не работает")
            return {'status': 'failed', 'error': 'Some error'}
            
    except Exception as e:
        print_fail(f"Ошибка: {e}")
        return {'status': 'failed', 'error': str(e)}

# Добавьте в test_basic_functionality() или создайте свою категорию
```

---

## Требования

- Python 3.7+
- requests library (`pip install requests`)

---

## Устранение проблем

### "ModuleNotFoundError: No module named 'requests'"
```bash
pip install requests
```

### "Connection timeout"
```bash
# Проверьте доступность backend
curl https://functions.poehali.dev/4b9b82b8-34d8-43e7-a9ac-c3cb0bd67fb1

# Увеличьте timeout в скрипте (по умолчанию 10 сек)
```

### "Permission denied"
```bash
chmod +x scripts/system-audit.py
```

---

## Контакты

Если обнаружили критические проблемы:
1. Сохраните отчет `audit_report_*.json`
2. Опишите проблему
3. Приложите отчет

---

**Рекомендация**: Запускайте проверку после каждого значительного изменения в коде платежей или системы баллов!