# 📧 Триггерные Email-рассылки

## Что сделано

### 1. Обновлены контакты
✅ **Frontend (Footer)** — убраны телефон и телеграм, оставлена только почта + ВК группа  
✅ **Backend emails** — все письма обновлены (приветственное, первое пополнение, восстановление пароля)

### 2. Созданы триггерные письма

Реализовано 3 типа автоматических писем для мотивации пользователей:

#### 1️⃣ Напоминание о пополнении (через 48 часов после регистрации)
**Когда:** Пользователь зарегистрировался, но не пополнил баланс  
**Цель:** Мотивировать сделать первое пополнение  
**Контент:**
- Преимущества платформы (дешевле заказа, мгновенно, 500+ работ)
- Акция: при пополнении от 500₽ → бонус +20% + промокод на 100 баллов
- Кнопка "Пополнить баланс"

#### 2️⃣ Брошенное избранное (через 3 дня)
**Когда:** Пользователь добавил работы в избранное, но не купил  
**Цель:** Напомнить о сохранённых работах  
**Контент:**
- Список избранных работ (до 3 штук)
- Напоминание, что популярные работы быстро разбирают
- Кнопка "Открыть избранное"

#### 3️⃣ Реактивация неактивных (14 дней без входа)
**Когда:** Пользователь не заходил 2 недели  
**Цель:** Вернуть на платформу  
**Контент:**
- Что нового появилось (50+ работ, защитный пакет, улучшения)
- Популярные категории сейчас
- Кнопка "Посмотреть новинки"

---

## Как запускать письма

### Вариант 1: Ручной запуск через Python скрипт

1. Открой файл `trigger-emails-cron.py`
2. Заполни настройки в начале файла:
```python
DATABASE_URL = "postgresql://..."  # DSN из env переменной DATABASE_URL
RESEND_API_KEY = "re_..."  # API ключ из env переменной RESEND_API_KEY
```

3. Установи зависимости:
```bash
pip install psycopg2-binary resend
```

4. Запусти:
```bash
# Все письма
python trigger-emails-cron.py all

# Только напоминания о пополнении
python trigger-emails-cron.py payment

# Только брошенное избранное
python trigger-emails-cron.py favorites

# Только реактивация неактивных
python trigger-emails-cron.py inactive
```

### Вариант 2: Автоматизация через cron (рекомендуется)

Добавь в crontab на сервере:

```bash
# Напоминание о пополнении (каждые 6 часов)
0 */6 * * * cd /path/to/project && python trigger-emails-cron.py payment >> /var/log/trigger-emails.log 2>&1

# Брошенное избранное (раз в день в 10:00)
0 10 * * * cd /path/to/project && python trigger-emails-cron.py favorites >> /var/log/trigger-emails.log 2>&1

# Реактивация (раз в день в 12:00)
0 12 * * * cd /path/to/project && python trigger-emails-cron.py inactive >> /var/log/trigger-emails.log 2>&1
```

---

## Технические детали

### Отправка писем
- **Сервис:** Resend API
- **От кого:** `Tech Forma <noreply@techforma.pro>`
- **Шаблоны:** HTML с адаптивной вёрсткой, градиенты, кнопки

### Логика триггеров

**Напоминание о пополнении:**
```sql
-- Пользователи 48±1 час после регистрации БЕЗ пополнений
SELECT u.* FROM users u
WHERE u.created_at BETWEEN NOW() - INTERVAL '49 hours' AND NOW() - INTERVAL '47 hours'
  AND NOT EXISTS (SELECT 1 FROM transactions t WHERE t.user_id = u.id AND t.type = 'deposit')
```

**Брошенное избранное:**
```sql
-- Пользователи с избранным старше 3 дней, которое не купили
SELECT u.*, json_agg(works) FROM users u
JOIN favorites f ON u.id = f.user_id
WHERE f.added_at < NOW() - INTERVAL '3 days'
  AND NOT EXISTS (SELECT 1 FROM user_purchases up WHERE up.work_id = f.work_id)
```

**Реактивация:**
```sql
-- Пользователи без входа 14+ дней
SELECT u.* FROM users u
WHERE COALESCE(u.last_login_at, u.created_at) < NOW() - INTERVAL '14 days'
```

### Безопасность
- Все письма отправляются только зарегистрированным пользователям с подтверждённой почтой
- Каждое письмо отправляется ОДИН раз (триггер срабатывает в узком временном окне)
- Кнопки отписки добавляются автоматически через Resend

---

## Мониторинг и статистика

### Логи отправки
Скрипт выводит статистику:
```
📧 Payment reminders: 12/15    (отправлено / найдено)
💝 Favorites reminders: 8/10
👋 Reactivation emails: 23/25
✅ Готово!
```

### В Resend Dashboard
- Открытия писем
- Клики по ссылкам
- Отписки
- Bounce rate

### Метрики эффективности
Отслеживай в админке:
- Conversion rate (письмо → пополнение)
- Click-through rate (письмо → переход на сайт)
- Reactivation rate (неактивный → активный)

---

## Рекомендации

### Частота отправки
- **Не спамить!** Одно письмо в 7-14 дней на пользователя
- Напоминание о пополнении: только 1 раз через 48ч
- Избранное: раз в 3-7 дней (настраивается в SQL)
- Реактивация: раз в 14-30 дней

### A/B тесты
Попробуй разные варианты:
- Время отправки (утро vs вечер)
- Текст subject (эмодзи vs без)
- Размер бонуса в акции (20% vs 30%)

### Оптимизация
Если conversion низкий:
1. Проверь, попадают ли письма в спам
2. Сделай subject короче и ярче
3. Упрости дизайн (меньше текста, больше кнопок)
4. Добавь срочность ("Акция 24 часа!")

---

## Контакты в письмах

**Текущие контакты (после обновления):**
- ✅ Email: tech.forma@yandex.ru
- ✅ ВК группа: vk.com/club234274626
- ❌ Телефон: УБРАН
- ❌ Телеграм: УБРАН

Это же отображается в футере сайта.
