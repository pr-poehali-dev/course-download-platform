# ❓ Часто задаваемые вопросы (FAQ)

## 🔧 Установка и настройка

### Q: Где взять Yandex S3 credentials?
**A:** 
1. Открой Yandex Cloud Console: https://console.cloud.yandex.ru/
2. Перейди в Object Storage
3. Слева найди "Сервисные аккаунты"
4. Создай новый (если нет) или открой существующий
5. Нажми "Создать новый ключ" → "Статический ключ доступа"
6. Скопируй **Идентификатор ключа** (KEY_ID) и **Секретный ключ** (SECRET_KEY)

### Q: Где взять DATABASE_URL?
**A:** Формат строки подключения PostgreSQL:
```
postgresql://username:password@host:port/database_name
```

Пример:
```
postgresql://admin:mypass123@postgres.example.com:5432/techforma
```

Если используешь Yandex Managed PostgreSQL:
```
postgresql://user:pass@c-xxx.rw.mdb.yandexcloud.net:6432/dbname
```

### Q: Как проверить что всё настроено правильно?
**A:** Запусти скрипт:
```bash
python convert_rar_to_zip.py
```

Если настройки неверные, увидишь конкретную ошибку:
- "Access Denied" → проверь S3 credentials
- "Connection refused" → проверь DATABASE_URL
- "Cannot find working tool" → установи unrar

---

## 🚀 Конвертация

### Q: Сколько времени займёт конвертация?
**A:** 
- **485 файлов:** ~15-20 минут
- **Средняя скорость:** 1.5-2 сек на файл
- **Зависит от:** скорости интернета и размера файлов

### Q: Сколько трафика потребуется?
**A:**
- **Скачать:** ~2.5-5 ГБ (RAR файлы)
- **Загрузить:** ~2.5-5 ГБ (ZIP файлы)
- **Итого:** ~5-10 ГБ общего трафика

### Q: Можно ли прервать конвертацию?
**A:** Да! Нажми `Ctrl+C` в любой момент. При повторном запуске скрипт пропустит уже обработанные файлы.

### Q: Что если у меня медленный интернет?
**A:** Используй пакетную версию:
```bash
python convert_rar_batch.py
```
Она обрабатывает по 50 файлов за раз с паузами.

### Q: Удалятся ли оригинальные RAR файлы?
**A:** **НЕТ!** Оригиналы остаются в облаке. ZIP создаются рядом:
- До: `kyra/work.rar`
- После: `kyra/work.rar` (остаётся) + `kyra/work.zip` (новый)

### Q: Что если некоторые RAR битые?
**A:** Скрипт их пропустит, выведет ошибку и продолжит работу. В конце увидишь полный список ошибок.

---

## 🐛 Проблемы и ошибки

### Q: "Cannot find working tool"
**A:** Не установлен unrar. Установи:
- **MacOS:** `brew install unrar`
- **Ubuntu:** `sudo apt-get install unrar`
- **Windows:** Скачай WinRAR и добавь в PATH

Проверь: `unrar --version`

### Q: "Access Denied" или "Invalid credentials"
**A:** Неверные S3 credentials. Проверь:
1. KEY_ID точно скопирован (без пробелов)
2. SECRET_KEY точно скопирован
3. У сервисного аккаунта есть права на bucket `kyra`

### Q: "Connection refused" или "Could not connect to server"
**A:** Проблема с DATABASE_URL. Проверь:
1. Хост и порт правильные
2. База данных запущена и доступна
3. Пароль верный
4. Если есть firewall, открыт порт PostgreSQL

### Q: "Memory error" или "Out of memory"
**A:** Слишком большой файл. Попробуй:
1. Закрыть другие программы
2. Использовать пакетную версию
3. Увеличить swap (Linux)

### Q: Скрипт завис на одном файле
**A:** Возможно очень большой архив. Подожди 5-10 минут или нажми `Ctrl+C` и перезапусти (файл будет пропущен).

### Q: "SSL: CERTIFICATE_VERIFY_FAILED"
**A:** Проблема с SSL сертификатами:
```bash
pip install --upgrade certifi
```

Или добавь в скрипт:
```python
import ssl
ssl._create_default_https_context = ssl._create_unverified_context
```

---

## 📊 После конвертации

### Q: Как проверить что конвертация прошла успешно?
**A:** Выполни SQL запрос:
```sql
SELECT 
  COUNT(*) FILTER (WHERE download_url LIKE '%.rar') as rar_count,
  COUNT(*) FILTER (WHERE download_url LIKE '%.zip') as zip_count
FROM works 
WHERE preview_image_url IS NULL;
```

Должно быть: `rar_count: 0-2`, `zip_count: 483-485`

### Q: Что делать после конвертации?
**A:**
1. Открой сайт `/extract-previews`
2. Нажми кнопку "Запустить извлечение превью"
3. Подожди 3-5 минут
4. Проверь что превью появились в каталоге

### Q: Нужно ли удалять RAR файлы вручную?
**A:** Необязательно, но можно для экономии места:
1. Проверь что все ZIP созданы
2. Проверь что превью извлечены
3. Удали RAR через S3 консоль или скриптом

### Q: Что делать с новыми загрузками?
**A:** Попроси авторов загружать сразу в ZIP формате. Добавь проверку при загрузке:
```typescript
if (!file.name.endsWith('.zip')) {
  alert('Пожалуйста, загружайте файлы в формате ZIP');
}
```

---

## 🔄 Извлечение превью

### Q: Сколько времени займёт извлечение превью?
**A:**
- **485 файлов:** 3-5 минут
- **Обработка пакетами:** по 15 файлов
- **Скорость:** ~1 сек на файл

### Q: Можно ли прервать извлечение?
**A:** Да, но лучше дождаться конца. Если прервал - просто перезапусти, уже обработанные файлы будут пропущены.

### Q: Что если в архиве нет preview.png?
**A:** Функция возьмёт первый PNG файл, который найдёт. Если PNG нет вообще - в ошибках будет "No PNG found".

### Q: Какого размера будут превью?
**A:** Максимум 800px по ширине. Высота масштабируется пропорционально. Формат PNG с оптимизацией.

---

## 💰 Стоимость

### Q: Сколько это всё стоит?
**A:** Примерная стоимость для 485 файлов:
- **Yandex Object Storage:** ~₽5-10 за трафик
- **Yandex Cloud Functions:** ~₽1-2 за выполнение
- **PostgreSQL:** Входит в текущий план
- **Итого:** < ₽15 (~$0.15)

### Q: Будет ли постоянная стоимость?
**A:** Нет, это разовая операция. После конвертации всё работает бесплатно (в рамках текущих лимитов).

---

## 🛡️ Безопасность

### Q: Безопасно ли вводить credentials в скрипт?
**A:** Да, скрипт работает локально на твоём компьютере. Credentials не отправляются никуда кроме Yandex Cloud.

**Рекомендации:**
- Не коммитьте файл с credentials в Git
- После использования можно удалить ключи
- Используйте сервисный аккаунт с минимальными правами

### Q: Что если скрипт удалит мои данные?
**A:** Скрипт **НЕ УДАЛЯЕТ** ничего. Он только:
1. Скачивает RAR
2. Конвертирует в ZIP
3. Загружает ZIP рядом
4. Обновляет ссылку в БД

Оригинальные RAR остаются нетронутыми.

### Q: Можно ли откатить изменения?
**A:** Да! Если что-то пошло не так:
1. RAR файлы всё ещё в облаке
2. Можно вернуть старые ссылки в БД:
```sql
UPDATE works 
SET download_url = REPLACE(download_url, '.zip', '.rar')
WHERE download_url LIKE '%.zip';
```

---

## 🎯 Производительность

### Q: Можно ли ускорить конвертацию?
**A:** Да, несколько способов:
1. Используй проводное подключение (быстрее Wi-Fi)
2. Запускай ночью (меньше нагрузка на сеть)
3. Увеличь BATCH_SIZE в `convert_rar_batch.py`

### Q: Можно ли распараллелить процесс?
**A:** Технически да, но не рекомендуется:
- Создаст большую нагрузку на S3
- Может вызвать rate limiting
- Усложнит обработку ошибок

### Q: Почему так долго?
**A:** Узкие места:
- Скачивание с S3: ~0.5 сек
- Распаковка RAR: ~0.3 сек
- Упаковка ZIP: ~0.3 сек  
- Загрузка в S3: ~0.4 сек

Основное время уходит на сетевые операции.

---

## 📱 Поддержка

### Q: Что делать если ничего не помогает?
**A:**
1. Проверь все настройки заново
2. Посмотри полный текст ошибки
3. Проверь логи в консоли
4. Напиши мне с описанием проблемы и логами

### Q: Где найти логи?
**A:**
- **Локальный скрипт:** Всё выводится в терминал
- **Cloud Function:** `/extract-previews` показывает прогресс
- **Backend логи:** Используй `get_logs` tool

### Q: Можно ли запустить на Windows?
**A:** Да! Но нужно:
1. Установить Python 3.11+
2. Установить WinRAR и добавить в PATH
3. Установить все зависимости через pip

Лучше использовать WSL (Ubuntu on Windows) или Git Bash.

---

## 🎉 Успех!

### Q: Как узнать что всё работает?
**A:** Признаки успеха:
- ✅ В БД: 485 работ с `download_url` на `.zip`
- ✅ В БД: 485 работ с `preview_image_url`
- ✅ На сайте: Превью отображаются в каталоге
- ✅ В S3: Папка `previews/` содержит 485 PNG файлов

### Q: Что дальше?
**A:**
1. Наслаждайся автоматическими превью! 🎉
2. Настрой требование ZIP для новых работ
3. Можешь удалить старые RAR (необязательно)
4. Система теперь работает полностью автоматически

---

**Остались вопросы? Спрашивай! 🚀**
