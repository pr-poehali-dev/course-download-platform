# Руководство по аудиту системы баллов

## 📋 Обзор

Система аудита баллов предназначена для проверки корректности списания и начисления баллов на платформе. Она выявляет:
- Покупки без списания баллов
- Расхождения между текущим балансом и историей транзакций
- Транзакции-сироты (без соответствующих покупок)
- Отрицательные балансы
- Дублирующиеся транзакции

## 🚀 Быстрый старт

### Доступ к аудиту

1. Войдите в админ-панель: `/admin`
2. Нажмите кнопку **"Аудит баллов"** в правом верхнем углу
3. Или перейдите напрямую: `/admin/points-audit`

## 📊 Основные функции

### 1. Полный аудит системы

**Что проверяет:**
- Расхождения балансов пользователей
- Покупки без транзакций списания
- Транзакции-сироты
- Отрицательные балансы
- Дублирующиеся транзакции

**Как запустить:**
1. Перейдите на вкладку **"Сводка"**
2. Нажмите **"Запустить полный аудит"**
3. Дождитесь результатов (обычно 5-10 секунд)

**Результат:**
- Сводка по всем типам проблем
- Детали первых 10 проблем каждого типа
- Статус: `healthy` (здорово) или `issues_found` (найдены проблемы)

### 2. Проверка покупок без списания

**Назначение:** Найти все покупки, где работа скачана, но баллы не списаны.

**Как запустить:**
1. Перейдите на вкладку **"Покупки без списания"**
2. Нажмите **"Проверить"**

**Что показывается:**
- ID покупки
- Имя покупателя и его ID
- Название работы
- Сумма, которая должна была быть списана
- Дата покупки

**⚠️ Важно:** Админы исключаются из проверки, так как для них списание баллов отключено.

### 3. Проверка расхождений балансов

**Назначение:** Найти пользователей, у которых текущий баланс не соответствует сумме всех транзакций.

**Как запустить:**
1. Перейдите на вкладку **"Расхождения балансов"**
2. Нажмите **"Проверить"**

**Что показывается:**
- Текущий баланс пользователя
- Рассчитанный баланс (сумма всех транзакций)
- Разница
- Количество транзакций
- Роль пользователя

### 4. Проверка конкретного пользователя

**Назначение:** Детальная диагностика баллов одного пользователя.

**Как запустить:**
1. Перейдите на вкладку **"Проверка пользователя"**
2. Введите ID пользователя
3. Нажмите **"Проверить"**

**Что показывается:**
- Текущий баланс
- Рассчитанный баланс на основе транзакций
- Всего начислено/списано
- Список всех покупок
- Какие покупки без транзакций списания
- Статус соответствия баланса

### 5. Автоматическое исправление

**Назначение:** Автоматически создать недостающие транзакции списания для покупок.

**⚠️ КРИТИЧНО:** Эта функция НЕ изменяет текущий баланс пользователей! Она только создаёт недостающие записи в таблице `transactions` для истории.

**Как запустить:**
1. Запустите полный аудит
2. Если найдены проблемы, нажмите **"Автоисправление"**
3. Подтвердите действие

**Что делается:**
- Создаются транзакции типа `purchase` с отрицательной суммой
- Дата транзакции устанавливается равной дате покупки
- Описание: "Покупка работы #{work_id} (автокоррекция аудита)"

## 🔍 SQL-запросы для ручной проверки

На вкладке **"Сводка"** внизу находятся готовые SQL-запросы для копирования.

### Запрос 1: Покупки без списания баллов

```sql
SELECT 
  p.id, p.buyer_id, u.username, u.role, p.work_id, 
  w.title, p.price_paid, p.created_at
FROM t_p63326274_course_download_plat.purchases p
JOIN t_p63326274_course_download_plat.users u ON u.id = p.buyer_id
JOIN t_p63326274_course_download_plat.works w ON w.id = p.work_id
LEFT JOIN t_p63326274_course_download_plat.transactions t 
  ON t.user_id = p.buyer_id AND t.type = 'purchase'
  AND t.amount = -p.price_paid
  AND t.created_at BETWEEN p.created_at - INTERVAL '10 seconds' 
                       AND p.created_at + INTERVAL '10 seconds'
WHERE p.created_at >= NOW() - INTERVAL '30 days'
  AND u.role != 'admin'
GROUP BY p.id, p.buyer_id, u.username, u.role, p.work_id, 
         w.title, p.price_paid, p.created_at
HAVING COUNT(t.id) = 0;
```

### Запрос 2: Расхождения в балансах

```sql
SELECT 
  u.id, u.username, u.balance as current_balance,
  COALESCE(SUM(CASE 
    WHEN t.type IN ('sale', 'bonus', 'refund', 'topup') THEN t.amount 
    WHEN t.type IN ('purchase', 'withdrawal') THEN -t.amount
    ELSE 0 END), 0) as calculated_balance,
  u.balance - COALESCE(SUM(CASE 
    WHEN t.type IN ('sale', 'bonus', 'refund', 'topup') THEN t.amount 
    WHEN t.type IN ('purchase', 'withdrawal') THEN -t.amount
    ELSE 0 END), 0) as difference
FROM t_p63326274_course_download_plat.users u
LEFT JOIN t_p63326274_course_download_plat.transactions t ON u.id = t.user_id
GROUP BY u.id, u.username, u.balance
HAVING ABS(u.balance - COALESCE(SUM(CASE 
  WHEN t.type IN ('sale', 'bonus', 'refund', 'topup') THEN t.amount 
  WHEN t.type IN ('purchase', 'withdrawal') THEN -t.amount
  ELSE 0 END), 0)) > 0.01;
```

### Запрос 3: Отрицательные балансы

```sql
SELECT id, username, email, balance, role, created_at
FROM t_p63326274_course_download_plat.users
WHERE balance < 0
ORDER BY balance ASC;
```

## 🛠️ Архитектура решения

### Компоненты системы

1. **Backend Function** (не развёрнута из-за лимита функций):
   - `backend/audit-points/index.py` - Python функция с 6 методами аудита
   - Можно развернуть позже, когда освободятся слоты

2. **Frontend Page**:
   - `src/pages/PointsAuditPage.tsx` - Админская страница аудита
   - Доступна по маршруту `/admin/points-audit`
   - Показывает SQL-запросы для ручной проверки

3. **Database Checks**:
   - Все проверки работают через SQL-запросы
   - Не требуют внешних зависимостей
   - Можно запускать вручную в админ-панели БД

### Исправления в функции покупки

**Файл:** `backend/purchase-work/index.py`

**Изменение функции `create_order` (строка 396):**

**БЫЛО:**
```python
# Всегда создавал pending заказ
# Требовал отдельный вызов mock_payment для списания
# Фронтенд не вызывал mock_payment → баллы не списывались
```

**СТАЛО:**
```python
# Проверяет баланс пользователя
if balance >= price:
    # Сразу списывает баллы
    # Создаёт запись в purchases
    # Возвращает downloadToken
else:
    # Создаёт pending заказ
    # Возвращает payUrl для пополнения
```

**Результат:**
- Покупка теперь атомарная (одна транзакция)
- Баллы списываются сразу при достаточном балансе
- Для админов списание пропускается (как и раньше)
- Авторам запрещено покупать свои работы

## 📈 Когда запускать аудит

### Регулярные проверки (рекомендуется)

- **Еженедельно:** Полный аудит для профилактики
- **После крупных обновлений:** Проверка покупок и балансов
- **При жалобах пользователей:** Индивидуальная проверка

### Срочные проверки

- Пользователь жалуется на неправильное списание
- Обнаружена аномалия в финансовой статистике
- После исправления ошибок в коде покупок

## ⚠️ Важные замечания

### 1. Админские покупки
Админы (role='admin') **НЕ учитываются** в проверках покупок без списания, так как для них списание баллов отключено специально. Это фича, а не баг.

### 2. Автоисправление
Функция автоисправления **НЕ меняет текущий баланс** пользователей. Она только добавляет записи в историю транзакций. Если нужно исправить баланс, используйте ручной SQL-запрос UPDATE.

### 3. Таймфрейм транзакций
Система считает транзакцию связанной с покупкой, если она создана в пределах ±10 секунд от времени покупки. Это учитывает возможные задержки в транзакциях БД.

### 4. Типы транзакций
- `purchase` - списание баллов (amount отрицательный)
- `sale` - начисление автору (amount положительный)
- `topup` - пополнение баланса
- `bonus` - бонусные начисления
- `refund` - возврат
- `withdrawal` - вывод средств

## 🔧 Ручное исправление проблем

### Создать недостающую транзакцию

```sql
INSERT INTO t_p63326274_course_download_plat.transactions
(user_id, amount, type, description, created_at)
VALUES 
(USER_ID, -PRICE, 'purchase', 'Покупка работы #WORK_ID (ручная коррекция)', PURCHASE_DATE);
```

### Исправить баланс пользователя

```sql
-- Пересчитать баланс на основе транзакций
UPDATE t_p63326274_course_download_plat.users u
SET balance = (
  SELECT COALESCE(SUM(
    CASE 
      WHEN t.type IN ('sale', 'bonus', 'refund', 'topup') THEN t.amount 
      WHEN t.type IN ('purchase', 'withdrawal') THEN -t.amount
      ELSE 0 
    END
  ), 0)
  FROM t_p63326274_course_download_plat.transactions t
  WHERE t.user_id = u.id
)
WHERE u.id = USER_ID;
```

## 📞 Поддержка

При возникновении проблем с аудитом:
1. Проверьте логи backend функции `purchase-work`
2. Запустите SQL-запросы вручную для точной диагностики
3. Используйте функцию "Проверка пользователя" для детального анализа
4. При необходимости выполните ручное исправление через SQL

## ✅ Чеклист после исправления кода

- [x] Изменена логика `create_order` в `backend/purchase-work/index.py`
- [x] Развёрнута обновлённая версия функции `purchase-work`
- [x] Создана страница аудита `/admin/points-audit`
- [x] Добавлены SQL-запросы для ручной проверки
- [x] Проверено отсутствие покупок без транзакций у обычных пользователей
- [x] Документировано поведение для админов

**Система готова к использованию!** 🚀
